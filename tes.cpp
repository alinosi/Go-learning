class Solution {
public:
    bool isPalindrome(int x) {
        string strx = to_string(x);
        if (strx.front() == strx.back()) {
            if ( strx.size()-2 % 2 == 0) {
                return true;
            } else if ()
                
        }
    }
};